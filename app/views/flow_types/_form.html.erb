<%= form_for(@flow_type) do |f| %>
  <% if @flow_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@flow_type.errors.count, "error") %> prohibited this flow_type from being saved:</h2>

      <ul>
      <% @flow_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :code %><br />
    <%= f.text_field :code %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
