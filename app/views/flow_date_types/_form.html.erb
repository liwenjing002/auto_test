<%= form_for(@flow_date_type) do |f| %>
  <% if @flow_date_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@flow_date_type.errors.count, "error") %> prohibited this flow_date_type from being saved:</h2>

      <ul>
      <% @flow_date_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :code %><br />
    <%= f.text_field :code %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
