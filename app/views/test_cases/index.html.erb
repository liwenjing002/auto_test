<h1>测试用例管理</h1>

<table>
  <tr>
    <th>标题</th>
    <th>所属用户</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @test_cases.each do |test_case| %>
  <tr>
    <td><%= test_case.title %></td>
    <td><%= test_case.user_id %></td>
    <td><%= link_to '查看用例', test_case %></td>
    <td><%= link_to '编辑', edit_test_case_path(test_case) %></td>
    <td><%= link_to '生成脚本', pro_test_cases_path(test_case) %></td>
    <td><%= link_to '执行脚本', excuse_test_cases_path(test_case) %></td>
    <td><%= link_to '删除用例', test_case, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to '创建用例', new_test_case_path %>
